{
  "name": "rescript-nact",
  "version": "5.0.0",
  "description": "let rescript-nact = (node.js, rescript, actors) ⇒ your µ services have never been so typed",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cometkim/rescript-nact.git"
  },
  "author": {
    "name": "Nick Cuthbert",
    "homepage": "https://github.com/ncthbrt"
  },
  "bugs": {
    "url": "https://github.com/cometkim/rescript-nact/issues"
  },
  "scripts": {
    "build": "rescript build",
    "start": "rescript build -w",
    "clean": "rescript clean",
    "test": "jest",
    "test-coverage": "jest --coverage && coveralls < coverage/lcov.info",
    "test-watch": "jest --watch",
    "pretest": "yarn run build",
    "prepush": "yarn run build && yarn run test",
    "prepack": "yarn run clean && yarn run build"
  },
  "keywords": [
    "rescript",
    "nact",
    "actors",
    "actor-model",
    "akka",
    "akka.net",
    "non-blocking",
    "actor system",
    "erlang",
    "event sourcing",
    "distributed-systems"
  ],
  "devDependencies": {
    "@glennsl/bs-jest": "^0.7.0",
    "@glennsl/bs-json": "^5.0.2",
    "coveralls": "^3.1.1",
    "husky": "^6.0.0",
    "jest": "^27.0.6"
  },
  "dependencies": {
    "bs-express": "^1.0.2",
    "nact": "^7.6.0",
    "rescript": "^9.1.4"
  }
}
